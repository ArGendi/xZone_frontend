import 'package:flutter/cupertino.dart';

class colorzone {
  Color firstColor;
  Color secondColor;
  colorzone(Color first, Color second) {
    this.firstColor = first;
    this.secondColor = second;
  }
}
